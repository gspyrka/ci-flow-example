# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
permissions:
  contents: read
  packages: write # A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - uses: astral-sh/ruff-action@v4.1.0
      with:
        src: "./src"
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4
    - name: Set image tag
      run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> "$GITHUB_ENV"
    - name: Build and export to Docker
      uses: docker/build-push-action@v7
      with:
        load: true
        tags: sample-api-${{ github.sha }}

    - name: Trivy debug
      uses: aquasecurity/trivy-action@v0.36.0
      with:
        image-ref: sample-api-${{ github.sha }}
    - name: Trivy debug
      uses: aquasecurity/trivy-action@v0.36.0
      with:
        image-ref: ghcr.io/astral-sh/uv:python3.14-trixie-slim
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@v0.36.0
      with:
        image-ref: sample-api-${{ github.sha }}
        format: 'table'
        exit-code: '1'
        ignore-unfixed: true
        vuln-type: 'os,library'
        severity: 'CRITICAL,HIGH'
